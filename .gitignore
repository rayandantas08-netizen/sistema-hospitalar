# =========================
# Backend (raiz do repo)
# =========================
/.env
node_modules/
dist

# Arquivos que não devem ser versionados
prontuario.controller.spec.ts
reqs

# OBS: jest.config.js deixou de ser ignorado — agora versionamos a configuração
# de teste em jest.config.js + tests/ (npm test roda 61 testes).

# Logs
combined.log
error.log

# IDE
.idea
